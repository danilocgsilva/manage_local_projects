{% extends 'base.html.twig' %}

{% block title %}Create a deploy receipt

    {% if project|default(null) %}
for project {{project.name}}
    {% endif %}

{% endblock %}

{% block h1 %}Create a deploy receipt

    {% if project|default(null) %}
for project {{project.name}}
    {% endif %}

{% endblock %}

{% block body %}
    {{ form(form) }}
{% endblock %}
