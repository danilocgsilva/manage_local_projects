# Documentation for devevelopment

By default, you needs to provides a command as the first argument and the project name as second argument.

All commands are actionable by `commands_bag.py` file. There's a dict with the same file name, in on which each key matches exactly the command inputted by the user.